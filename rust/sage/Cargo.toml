[package]
name = "sage"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
abi_stable = "0.11.1"
steel-core = { path = "../../../steel/crates/steel-core", features = ["dylibs"] }